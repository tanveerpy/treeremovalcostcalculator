const nextConfig = {
    output: 'export',
    basePath: '/treeremovalcostcalculator',
    images: {
        unoptimized: true,
    },
    // Configured for optimized build and deployment
}

module.exports = nextConfig
